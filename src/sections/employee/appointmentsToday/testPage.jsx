import { Checkbox } from '@mui/material';
import FormControlLabel from '@mui/material/FormControlLabel';
// defaultChecked
export default function testPage() {
  return <FormControlLabel label="Diabetes" control={<Checkbox size="small"  />} />;
}
