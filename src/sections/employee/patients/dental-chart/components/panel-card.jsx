// Promoted to src/components/panel-card once the patient profile needed it too.
// Kept here as a re-export so the dental-chart panels' imports stay put.
export { default } from 'src/components/panel-card';
